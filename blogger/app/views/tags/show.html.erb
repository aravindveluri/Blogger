<h1>Articles Tagged with <%= @tag.name %></h1>

<ul class = "artList">
  <% @tag.articles.each do |article| %>
    <li class = "art"><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>
